import styled from 'styled-components/native';
import { Title } from 'react-native-paper';

const StyledTitulo = styled(Title)`
    margin-horizontal: 16;
    font-size: 20;
    font-weight: 500;
    color: rgba(0, 0, 0, 0.6)
`;
export default StyledTitulo;
