import styled from 'styled-components/native';
import { Text } from 'react-native';

const StyledTexto = styled(Text)`
    font-size: 14;
    color: rgba(0, 0, 0, 0.6)
`;

const StyledTitulo = styled(Text)`
    font-size: 18;
    color: rgba(0, 0, 0, 1)
`;

export { StyledTexto, StyledTitulo };
